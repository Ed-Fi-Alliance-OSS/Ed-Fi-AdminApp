meta {
  name: app-healthcheck
  type: http
  seq: 1
  tags: App
}

get {
  url: {{BASE_URL}}/healthcheck
}

tests {
  test("status is 200 or 404", function() {
    const status = res.getStatus();
    expect([200, 404, 502]).to.include(status);
  });
}
