meta {
  name: auth-me
  type: http
  seq: 1
  tags: Auth
}

headers {
  Authorization: Bearer {{ACCESS_TOKEN}}
}

get {
  url: {{BASE_URL}}/auth/me
}

tests {
  test("endpoint responds", function() {
    const status = res.getStatus();
    expect([200, 204]).to.include(status);
  });
}
