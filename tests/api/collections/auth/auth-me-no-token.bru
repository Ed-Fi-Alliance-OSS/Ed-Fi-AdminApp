meta {
  name: auth-me-no-token
  type: http
  seq: 2
  tags: Auth
}

get {
  url: {{BASE_URL}}/auth/me
}

tests {
  test("endpoint responds without token", function() {
    const status = res.getStatus();
    expect([401, 403]).to.include(status);
  });
}
