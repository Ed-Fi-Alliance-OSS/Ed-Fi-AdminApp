# Ignored vulnerabilities for OpenSSF Scorecard's "Vulnerabilities" check (osv-scanner).
# All entries below are devDependency-only (build/CI tooling), confirmed via
# `npm audit --omit=dev` returning 0 vulnerabilities, and have no upstream fix
# available as of the date noted. Re-check before this date and remove entries
# once a fix ships. eng/helpers/check-ignored-vuln-fixes.ps1 automates that check
# (also runs as an informational step in .github/workflows/scorecard.yml).

# --- image-size: via less -> @nx/webpack (devDependency, build tooling) ---
[[IgnoredVulns]]
id = "GHSA-5p2g-fcmc-qvqq"
reason = "image-size, via less -> @nx/webpack (devDependency, build tooling). No patched image-size version exists yet (latest 2.0.2 still flagged)."
ignoreUntil = 2026-10-05

[[IgnoredVulns]]
id = "GHSA-w3rx-r6r6-pgpr"
reason = "image-size, via less -> @nx/webpack (devDependency, build tooling). No patched image-size version exists yet (latest 2.0.2 still flagged)."
ignoreUntil = 2026-10-05
